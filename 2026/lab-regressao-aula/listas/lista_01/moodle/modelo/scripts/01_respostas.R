# Lista 1 - escreva os cálculos neste arquivo, em ordem de execução.
# Preserve os dados originais. Não use setwd() com um caminho do seu computador.
# Abra Lista_01.Rproj e use caminhos relativos à pasta do projeto.
options(scipen = 999)

# Carregue aqui os pacotes utilizados em sua solução.
# library(data.table)
# library(dplyr)
# library(ggplot2)
# library(here)

# Guarde tabelas e figuras nesta lista para incluí-las no relatório.
resultados <- list()

# 1. Importação e validação municipal -------------------------------------
# Registre a tentativa inicial e investigue o resultado, conforme o enunciado.
# Arquivo: dados/populacao_municipios_2020.csv
# Ao selecionar colunas, use dplyr::select().

# 2. Municípios de São Paulo ---------------------------------------------

# 3. Centro, dispersão e forma -------------------------------------------

# 4. Médias por UF e média nacional --------------------------------------

# 5. Probabilidades e simulação ------------------------------------------
# Arquivo: dados/brasil_convergencia_china_1997_2016.csv
# Use data.table::fread(), como no laboratório.

# 6. Esperança condicional e EQM -----------------------------------------

# 7. Melhor preditor linear ----------------------------------------------

# 8. Covariância zero não implica independência ---------------------------

# 9. Tema, período e síntese ---------------------------------------------

# Exemplos de organização dos seus próprios resultados (não são respostas):
# resultados$verificacoes <- tabela_de_verificacoes
# resultados$figura_1 <- grafico_criado_por_voce
# dir.create("output", showWarnings = FALSE)
# write.csv(sua_tabela, "output/tabela_1.csv", row.names = FALSE)
